// const CONFIG = {
//   API_HOST: "https://api.pplt.vitlab.site", // Change this when needed
// };

// export default CONFIG;


const CONFIG = {
  API_HOST: process.env.API_HOST, // Change this when needed
};

export default CONFIG;
